<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Proxy - Wildcard</title>
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body { font-family:Arial,sans-serif; background:#1a1a1a; color:#fff; overflow:hidden; }
        #frame { width:100vw; height:100vh; border:none; }
        .header { position:fixed; top:0; left:0; right:0; background:rgba(0,0,0,0.9); padding:10px; z-index:1000; }
        input[type=url] { width:70%; padding:8px; background:#333; color:#fff; border:1px solid #555; }
        button { padding:8px 16px; background:#007bff; color:white; border:none; cursor:pointer; }
        button:hover { background:#0056b3; }
    </style>
</head>
<body>
    <div class="header">
        <input type="url" id="url" placeholder="https://discord.com" value="https://discord.com">
        <button onclick="loadUrl()">Go</button>
    </div>
    <iframe id="frame" src="https://discord.com"></iframe>

    <script>
        function loadUrl() {
            const url = document.getElementById('url').value;
            document.getElementById('frame').src = url;
        }
        
        // Auto-load Discord
        window.onload = () => loadUrl();
        
        // Bypass common blocks
        window.onbeforeunload = null;
    </script>
</body>
</html>
